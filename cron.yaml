cron:
# - description: "Collecting statistics"
#   url: /tasks/statistics
#   schedule: every sunday 12:00
#   timezone: Europe/Moscow
#   retry_parameters:
#     job_retry_limit: 5
#     min_backoff_seconds: 600
 - description: "Filtering tasks"
   url: /tasks/popular_filtering
   schedule: every 4 hours
   retry_parameters:
     job_retry_limit: 3
     min_backoff_seconds: 60
     
